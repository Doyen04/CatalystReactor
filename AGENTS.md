# AGENTS.md

CanvasKit (Skia WASM) vector editor. React 19 + Vite 6 + Tailwind 4 + Zustand + TypeScript. React UI is thin; logic lives in `src/lib/{core,shapes,tools,modifiers,node}`.

Read `.github/copilot-instructions.md` for the **current** architecture (event flow, scene graph, layout containers, tool/shape extension recipes). It is accurate and detailed.

**The active refactor plan and target architecture:** `docs/architecture.md` (engine-authoritative, headless, command/journal-based). This is the map for all work on the `refactor/engine-architecture` branch — one branch, all planned changes land here. Target end-state layout: `src/{core,engine,bridge,ui}` tiers with a monotone import direction (see the plan's section 3). If a change is listed in the plan, implement it there; do not improvise a parallel sketch in `src/lib`. Default tool is `select`; per-group remembered tools exist.

## Commands

- Dev: `npm run dev`
- Build: `npm run build` (runs `tsc -b && vite build` — typecheck happens here)
- Lint: `npm run lint` (`eslint .`)
- Test: `npm run test` (`vitest run`); watch: `npm run test:watch` (`vitest`)
- No typecheck-only or prettier script; import ordering and `git diff` will not be checked by CI.

Formatting is Prettier but not wired to a script; run `npx prettier --write <files>`. Config: 4-space indent, no semicolons, single quotes, printWidth 150, `arrowParens: avoid`, trailing comma es5.

## Import aliases (quirk)

`vite-aliases` (`deep: true`) auto-generates an `@<dirname>` alias for every folder under `src` at dev/build time. `@/*` → `src/*`, e.g. `@lib/core/CanvasManager`, `@hooks/useTool`. New folders under `src` get aliases automatically — do NOT hardcode path config. `tsconfig.json` `paths` mirrors this for the editor's typechecking; note `tsconfig.app.json` overrides `paths` to `{}` (Vite still resolves aliases so builds work).

## Architecture boundaries (enforced direction)

Imports point one way only: `ui/` → `bridge/` → `engine/` → `core/` (plan sections 2.4 and 11). Information travels back up as **events**, never as imports.

- `src/engine/**` must stay headless: never import React, Zustand, `src/ui`, `src/hooks`, or `src/component`. `src/engine/render/` is the first engine tier and owns WASM resource lifecycles.
- `src/core/**` (once it exists) must not import CanvasKit.
- WASM resources are **not** garbage-collected. `Paint`/`Path`/`Paragraph`/`Shader`/`Image` ownership goes through `src/engine/render/{PaintCache,ResourceScope,TextCache}`. Never mutate a paint returned by `PaintManager.getPaint`/`makeNewPaint` — request a new descriptor instead.
- Engine/tier imports may use `@/*`, `@engine/...` (both present in `tsconfig.json` paths), or relative paths. Only auto-generated `@<folder>` aliases that are NOT present in `tsconfig.json` paths are unsafe — they break typechecking.

## Refactor status (`refactor/engine-architecture`)

- **Step 1 (stop the leaks) landed:** `src/engine/render/{PaintCache,TextCache,ResourceScope,ResourceCounter}` added; paint creation routed through `PaintCache`; PText paragraphs cached by version+width; shared-paint mutation removed from all live shapes/tools (only the preserved dead `Handles.ts` fallback still uses the getters); dev-only `[skia]` counter wired in `Canvas.tsx`.
- **Step 2 (delete the dead code) landed:** removed the unimplemented `freeform`/`scale` entries from `ToolBar`, dropped `BooleanAction` from `HistoryManager`, removed the `EventQueue` wiring (import + `removeAllEvent()` call) from `CanvasManager`, and quarantined the preserved dead files into `to-be-deleted/`.
- **Step 3 (the test harness) landed:** Vitest added as a devDependency; root `vitest.config.ts` derives `resolve.alias` from `tsconfig.json` `compilerOptions.paths`/`baseUrl` (deliberately no `vite-aliases` plugin — it spawns filesystem watchers that delay shutdown). Tests live in `src/lib/__tests__/*.test.ts`, run in a `node` environment (no CanvasKit/React/DOM). Coverage: pure math (`Vector`, `clamp`/`normalizeAngle`/`handleUtil`/`pointInArc`, `roundingUtil`, `LayoutEngine` row/column/grid incl. padding/align/auto-resize branches, `SnapManager` grid, `PCache`, `debounce`/`throttle`), state (`EngineStateStore`/`HistoryManager`), fills (`getBackgroundFill`), `DependencyManager`/`ResizeCursor`/`textUtil`, `TextEditor` span logic, and the engine render caches (`PaintCache`/`TextCache`/`ResourceScope`/`ResourceCounter`) via lightweight fakes — 12 files, 244 tests passing. Add tests alongside each subsequent step.
- **Step 4 (dirty-gated rendering) landed:** `src/engine/render/FrameScheduler.ts` (dirty flag + one coalesced `requestAnimationFrame`; `request`/`start`/`stop`, no Skia) and `src/engine/render/renderRequest.ts` (a process-wide `requestRender()` hook so non-render code can request a frame without importing the Renderer). `Renderer` no longer runs a continuous 60fps loop: it owns a `FrameScheduler`, registers `setRenderRequest(this.requestRender)` on construct and clears it on destroy, defers surface creation one rAF (tracked as `bootRafId`, cancelled on destroy), and re-reads `devicePixelRatio` in `makeSurface`. `requestRender()` is called from `ToolManager` pointer/key callbacks, `ShapeManager.attachNode/detachShape/updateProperty`, `HistoryManager.pushAction/undo/redo`, and the `TextCursor` blink interval. 13 tests added in `src/engine/__tests__/frameScheduler.test.ts` (fake rAF) — 13 files / 249 tests total. Runtime check (devtools: idle = no frames) still deferred.
- **Step 5 (ToolContext + the outbound bus) landed:** `src/engine/events/EngineBus.ts` (typed emitter; `on` returns an unsubscribe, `emit` iterates a snapshot, `clear`) and `src/lib/core/EngineEvents.ts` (`tool:changed` / `selection:changed` / `properties:changed`). `CanvasManager` owns one `bus` and passes it to `ShapeManager` and `ToolManager`. `ToolContext` (`src/lib/tools/ToolContext.ts`) is built once by `ToolManager` (`defaultTool`, `sceneManager`, `shapeManager`, `shapeModifier`, `setTool`, `setCursor`, `requestRender`); every `Tool`/subclass constructor is now `(cnvs, ctx)` and no longer resolves from `DependencyManager` or imports stores. `ToolManager.setCurrentTool` early-returns on the same tool and `ctx.setTool` emits `tool:changed`. `ShapeManager` takes `gridSize` via `setGridSize` (pushed from `Canvas.tsx` -> `CanvasManager.setGridSize`) and emits `selection:changed` / `properties:changed` instead of touching `useSceneStore`. `src/bridge/engineStoreBridge.ts` (`connectEngineToStores(bus)`) is the only bus<->Zustand seam; `Canvas.tsx` calls it and disconnects on cleanup. Acceptance: `useToolStore`/`useSceneStore`/`zustand` appear nowhere under `src/lib` or `src/engine`. Tests added: `src/engine/__tests__/engineBus.test.ts` (11) and `src/lib/__tests__/engineStoreBridge.test.ts` (7) — 15 files / 267 tests total.
- **Step 6 (the document model) landed** — `DocumentModel` is the headless source of truth; all five sub-steps are in:
    - **6.1** `src/engine/document/{entity,journal,DocumentModel,serialize}.ts`. `EntityRecord = { id, type, properties, parentId, version }`; journal entries are `props | insert | remove | reparent`; `setProperties` journals each changed key (in-place mutation, version bump, `Object.is` skip), `reorder` journals a same-parent `reparent` entry (no-op moves skip recording), `remove` is leaf-only, out-of-range indices are clamped. `toJSON`/`fromJSON` carry `formatVersion: 1`. Tests: `src/engine/__tests__/{documentModel,journal}.test.ts` (45 tests).
    - **6.2** `EngineStateStore` is now a **thin facade** over one private `DocumentModel` (shared via `EngineStateStore.getInstance().getDocument()`). Its old public methods (`createShapeData`, `getShapeData`, `getAllShapeData`, `removeShapeData`, `subscribe`, `notify`) are unchanged — it hands out live write-through `{ id, type, properties }` views whose setters route into `doc.setProperties`. `createShapeData` stores a `structuredClone` of the properties.
    - **6.3** `SceneManager` derives its tree from the doc: constructor is `(shapeModifier, shapeManager, doc: DocumentModel)` (CanvasManager passes `EngineStateStore.getInstance().getDocument()`). It keeps a stable `nodeById` map (one projection per entity id) and a journal subscription that re-runs reconciliation on every non-`props` entry. New API: `addShapeToScene(type, pos, image?)` (registers the record + materializes the projection node, `ContainerNode` for `plainRect`), `insertNode(node, parentId, index?)` (append on undefined/-1), `removeNode(id)` (doc remove + node destroy), `reorderNode(id, index)`, `getNode(id)`. Old methods (`getScene`, `getCollidedScene`, `flattenScene`, ...) read the projected tree.
    - **6.4** `ShapeFactory.createShapeFromData(data, image?)` builds a `Shape` from an existing `ShapeData` without registering/id-generating (pure type->class switch, throws on unknown type); `createShape` registers via the store and delegates.
    - **6.5** Shape/Line/Pen/Bezier/Image/Group/Select/Edit tools write only through `SceneManager` — no `new ShapeNode` / `scene.addChildNode` / `parent.removeChildNode` anywhere in `src/lib/tools`. EditTool flatten inserts the replacement at the old node's index and removes the old entity record.
    - Known Step 6 behaviour deltas: a shape drawn while a container is under the cursor registers at the doc root (containers are applied via GroupTool/SelectTool capture); `props` journal entries never re-run tree reconciliation.
- **Step 7 (journal-backed commands) landed** — undo is derived from the journal, not hand-written:
    - **Transactions on `DocumentModel`**: `beginTransaction` / `commitTransaction` (returns the journalled entries) / `abortTransaction` (applies the inverted entries in reverse to revert) / `applyEntry(entry)` (replays an entry's state effect + listener notification without journaling) / `setPropertiesExplicit(id, before, after)` (journals the diff between two property snapshots — the crux for drags that mutate the live properties object in place). While a transaction is open `record()` buffers into `pending` and only grows the flat journal on commit.
    - **`CommandManager`** (`src/engine/commands/CommandManager.ts`): `constructor(doc, bus)`; `run(cmd)` / `begin(label, mergeKey?)` / `apply(cmd)` / `commit()` / `abort()`; `undo()`/`redo()` replay journal inversions via `doc.applyEntry`; `canUndo`/`canRedo`; undo stack capped at 200 with same-`mergeKey` coalescing on consecutive commits; emits `history:changed` + `requestRender()` when the flags flip. Transaction shape: `{ label, mergeKey, entries, touched }`.
    - **Commands** in `src/engine/commands/{CreateShape,TranslateNodes,UpdateProperties,DeleteNodes,ReparentNodes,ReorderNodes,EditPath}.ts`; each describes only a forward mutation (journal provides the inverse). `CreateShape` generates its id internally and exposes `resultId` (headless-safe, no ShapeFactory/CanvasKit import); `UpdateProperties(id, oldProps | null, newProps)` uses `setPropertiesExplicit` with snapshots (drag path) and `setProperties` otherwise (panel path); `ReorderNodes` delegates to `doc.reorder`, which journals the same-parent `reparent` itself.
    - **Headless editor**: `src/engine/createEditor.ts` (`{ doc, run, undo, redo, canUndo, canRedo }`); §2.2 acceptance test in `src/engine/__tests__/headless.test.ts` (create → translate → undo → redo, no canvas/React/store).
    - **Live wiring**: `CanvasManager` creates one `CommandManager` (before shapeManager), `undo()`/`redo()` delegate to it. `ShapeManager` drags `begin('Move', 'translate:<id>')` on start, `apply(UpdateProperties)` + `commit()` on release (or `abort()` when unchanged); new `cancelDrag()` reverts mid-drag Escape; `updateProperty` runs `UpdateProperties`. `ToolContext.commandManager` injected from the manager wiring; `KeyboardTool` (now `(shapeManager, commandManager, doc)`) does Ctrl+Z/Ctrl+Y via the command manager and Escape via `shapeManager.cancelDrag()` when `doc.isTransactionOpen`. `HistoryManager.ts`/`UpdateShapeAction` moved to `to-be-deleted/lib/core/HistoryManager.ts`; its coverage was replaced by CommandManager tests in `src/lib/__tests__/engineState.test.ts`.
    - Tests: `src/engine/__tests__/{headless,commandManager,commands}.test.ts` (28) + transaction/setPropertiesExplicit additions to `documentModel.test.ts` — 20 files / 354 tests total. Manual smoke verification (create/group/reparent/edit-path/edit-text + Escape revert) still deferred.
- **Step 8 (React cut-over, duplicates deleted) landed** — the UI now reads/writes the `DocumentModel` directly:
    - **`document:changed` replaces `properties:changed`**: `EngineEvents` dropped `properties:changed`; `ShapeManager` emits `document:changed: { ids }` with the touched id on every frame during draws/drags/moves (no engine throttle); `CommandManager` emits it with the touched ids on `commit()` and on `undo()`/`redo()` (which also `requestRender()`).
    - **Bridge hooks** (`src/bridge/{editor,useEditor,EditorProvider,useEntity,useEntityThrottled,useDocumentRevision,propertyPatch}.ts(x)`): `makeEditor(canvasManager)` exposes `{ doc, bus, run, undo, redo, canUndo, canRedo }` (doc = `EngineStateStore.getDocument()`, commands = per-canvas `CommandManager`) and `EditorProvider` provides it above `useCanvasManagerStore`. **Caveat:** the doc mutates `EntityRecord`s in place, so `useEntity`/`useEntityThrottled` (50ms) do NOT use `useSyncExternalStore`'s `doc.get(id)` snapshot (identical identity would never re-render) — they force a re-render via a filtered bus subscription and read `doc.get(id)` fresh each render.
    - **`PropertyBar`**: `selectedShapeId` from `useSceneStore`, record via `useEntityThrottled`, writes via `commands.run(new UpdateProperties(id, null, patch))` where `patch` comes from pure `propertyPatch` builders (`patchProperty`/`patchBorderRadius`/`patchRadiusLock`/`patchStyle` — unit-tested in `src/lib/__tests__/propertyPatch.test.ts`).
    - **`LayersPanel`**: hierarchy from `doc.childrenOf(rootId)` recursively (container = `type === 'plainRect'` or has children; layout type from `layoutConstraints.type`); refresh via `useDocumentRevision()` (100ms); auto-expand via `doc.ancestorsOf(selectedShapeId)`; select via `sceneManager.getNode(id)` → `shapeManager.attachNode`. The old `EngineStateStore.subscribe` hack is gone.
    - **Deleted**: `useSceneStore.{currentShapeProperties,setCurrentShapeProperties,clearProperties}` + the `EngineStateStore.subscribe` side-effect in `sceneStore.tsx`; `ShapeManager.{throttledUpdate,updateProperty,updateSubProperty,updateStyle,updateBorderRadius,updateRadiusLock}` and its `notify()` calls; the `properties:changed` mapping in `engineStoreBridge.ts`; `src/lib/types/engine.ts`. The drag throttle now lives only at the bridge.
    - Tests: `propertyPatch.test.ts` (+24); `engineStoreBridge.test.ts` resized (no `currentShapeProperties`); CommandManager render-count test updated for the extra `document:changed` render request — 21 files / 379 tests total. Manual UI smoke (panel↔canvas round-trip + undo) still deferred.
- **Step 9 (per-editor instances) landed** — no module-level singletons remain except the intentional `CanvasKitResources`; `createEditor()` is the full composition root and the UI just mounts it:
    - `src/lib/core/DependencyManager.ts` was **deleted** — dependencies are explicit constructor arguments (plus the `ToolContext` tools already receive). `EngineStateStore` gained `constructor(doc)` (dropped `getInstance()`); `SnapManager` gained a public `constructor()` (dropped `getInstance()`).
    - `createEditor()` (`src/engine/createEditor.ts`) builds one per-editor `{ bus, doc, store, commandManager, snap }` and returns the full `Editor` (`doc`/`bus`/`store`/`isAttached`/`attach`/`detach`/`dispose`/`setTool`/`setGridSize`/`run`/`undo`/`redo`/`canUndo`/`canRedo`). It stays fully headless: `PaintManager` + `ShapeModifier` are constructed only inside `attach()` (they require CanvasKit), so the headless `headless.test.ts` never touches WASM. `attach` throws on double-attach; `dispose` = `detach()` (whose `CanvasManager.destroy()` tears down shapeModifier/paintManager/resource counters), so a never-attached editor leaks nothing.
    - Construction is explicit, no DI container: `Shape(data, paintManager)`, every primitive `(data, paintManager)` (`Image` adds `imageElem?`), `ContainerNode(shape, layoutConstraints, paintManager)`, `TextCursor(x, y, height, paintManager)`; `ShapeFactory.createShapeFromData(data, paintManager, image?)` / `createShape(type, pos, paintManager, store, image?)`; `ShapeModifier(paintManager)`; `ShapeManager(shapeModifier, bus, commandManager, doc, snap)` (no `EngineStateStore` import; `setGridSize` also pushes into `this.snap`); `SceneManager(shapeModifier, shapeManager, doc, paintManager, store)`; `ToolManager(cnvs, inputManager, bus, sceneManager, shapeManager, shapeModifier, commandManager, doc)` (KeyboardTool gets the injected `doc`); `ToolContext` is the only dependency surface for tools.
    - `src/bridge/editor.ts` was **deleted** — `EditorProvider.tsx` does `useState(() => createEditor())` (one provider, one editor) + `dispose()` on unmount; `useEditor()`/`useEntity`/... import `Editor` from `@/lib/engine/createEditor` directly (no `makeEditor(canvasManager)`). The `Editor` handle also exposes `sceneManager()` / `shapeManager()` (nullable while unattached), and `useCanvasManagerStore`/`CanvasManagerProvider` (React context) were **deleted** — `LayersPanel` reads the managers from `useEditor()`.
    - `Canvas.tsx` is a **mount point**: module-level `ensureCanvasKit()` lazily boots WASM once (shared `ckPromise`, reset on failure so it can retry); the boot effect awaits it, then `editor.attach(canvas)` → `setCanvasManager(manager)` → pushes the current tool/gridSize post-attach (initial values are no longer dropped) → registers the `[skia]` counter. StrictMode-safe via a local `cancelled` flag + `editor.isAttached()` guard (single attach/detach).
    - Tests: `src/engine/__tests__/perEditorIsolation.test.ts` (+2: two editors hold independent docs/buses/stores and create/undo/redo in one never touches the other); singleton tests rewritten over `new SnapManager()` / `new EngineStateStore(new DocumentModel())`; the `DependencyManager`/`ServiceContainer` tests were deleted; `headless.test.ts` green because `createEditor()` itself is CanvasKit-free — 22 files / 378 tests total (headless `sceneManager()`/`shapeManager()` accessors return null before attach). Two-side-by-side editors smoke check still deferred.
- `PaintManager.getPaint(request)` returns an immutable cached paint and is the preferred API. The legacy `paint`/`stroke` getters remain only for the quarantined `Handles.ts` reference (`to-be-deleted/lib/modifiers/Handles.ts`); nothing live uses them.

## Boot flow

`src/engine/createEditor.ts` is the full composition root: `createEditor()` builds one `EngineBus`, `DocumentModel`, `EngineStateStore(doc)`, `CommandManager`, and `SnapManager` per editor (the only process-wide singleton left is `CanvasKitResources`). `src/component/Canvas.tsx` is the mount point: it lazily boots `canvaskit-wasm` once (`ensureCanvasKit()`, module-level cached promise) and then `editor.attach(canvas)` constructs the CanvasKit-bound services (`PaintManager` → `ShapeModifier`) inside `attach()` and passes the `CanvasDeps` to `new CanvasManager(canvas, deps)`, which wires the managers in dependency order (`ShapeManager` → `SceneManager` → `InputManager` → `Renderer` → `ToolManager`). Everything is constructor-injected — there is no service container; `ToolContext` is the only dependency surface for tools. `PaintManager`/`ShapeModifier` are built lazily on attach so the headless `createEditor()` never touches WASM.

Events: `InputManager` is the only **input** path — no event bus for inbound events. It captures native pointer/key/resize events and fans them out to **direct subscriber callbacks** (`InputCallbacks`: `onPointerDown/Move/Up`, `onKeyDown/Up`, `onResize`); `ToolManager` and `Renderer` `subscribe()` to it. The old `EventQueue` class survives only as a reference copy in `to-be-deleted/lib/core/EventQueue.ts`; nothing under `src/` may import it. Outbound (engine -> UI) changes travel on the typed `EngineBus` (`CanvasManager.bus`) — consumed by `src/bridge/engineStoreBridge.ts` (`tool:changed` / `selection:changed`) and by the `EditorProvider` bridge hooks (`document:changed`); the engine still never imports Zustand/React. `setTool` creates a fresh tool instance each switch (same-type switches early-return).

Rendering is dirty-gated: `Renderer` owns a `FrameScheduler` and only draws when `requestRender()` (from `@/lib/engine/render/renderRequest`) is called — there is no continuous loop. `Renderer` registers itself as the process-wide render-request target on construct and clears it on destroy (the hook is a single slot, so it assumes one live canvas).

## Stores

- `useToolStore` (Zustand) — active/default tool, per-group remembered tool; `setDefaultTool()` returns to select.
- Engine live-accessors — the `Editor` handle (via `useEditor()`) exposes `sceneManager()` / `shapeManager()` (nullable while unattached) alongside `doc`/`bus`/`store`; there is no `useCanvasManagerStore`/context anymore. `LayersPanel` reads the managers from `useEditor()`.
- `useSceneStore` (Zustand) — `selectedShapeId`, `gridSize` (pushed to `ShapeManager.setGridSize`); `selectedShapeId` is updated from the engine only through `src/bridge/engineStoreBridge.ts`. Selection/panel content follows the engine via the `EditorProvider` bridge hooks (`useEntity`/`useEntityThrottled`), not a property copy.
- `EngineStateStore` (per-editor, `new EngineStateStore(doc)` built by `createEditor()`) — shape data map keyed by id; the shared source of truth bridges the canvas engine and React panels. **Any shape created via `ShapeFactory` registers here.**
- Undo/redo now lives in the engine: `CanvasManager.commandManager` (one `CommandManager` per editor, built in `createEditor()` and passed through `attach`). `CanvasManager.undo/redo` delegate to it; Ctrl+Z/Ctrl+Y are handled by `KeyboardTool`. The old `HistoryManager` is quarantined at `to-be-deleted/lib/core/HistoryManager.ts` (working reference copy; nothing under `src/` may import it).

## Gotchas

- Dev runs under `<StrictMode>`, so the Canvas bootstrap effect double-fires; `Canvas.tsx` guards re-init via refs. Keep that guard when touching boot/teardown.
- `import/no-cycle` is an eslint error (maxDepth Infinity) — the manager graph in `src/lib/core` can tempt cycles.
- Tool type union is `ToolType` in `src/lib/tools/toolTypes.ts`; it includes `ContainerType` (row/column/grid/frame/none) used by group tools.
- `Renderer` and tool code assume `CanvasKitResources` is already initialized; anything running before canvas boot will fail.
- `SnapManager` is per-editor (`new SnapManager()` in `createEditor()`), configured via `Canvas.tsx` → `CanvasManager.setGridSize` → `ShapeManager.setGridSize` (which also pushes into `snap.setConfiguration`). `ShapeManager` lazily caches its snap-guide paint/dash; that cache is per-ShapeManager, reset on re-mount.
- **The preserved dead/reference files are quarantined under `to-be-deleted/`** (excluded from tsconfig and eslint): `to-be-deleted/lib/core/{toImplement.ts,PathOperator.ts,EventQueue.ts,BooleanAction.ts,HistoryManager.ts}` and `to-be-deleted/lib/modifiers/{Handles.ts,modifier.ts,modifierUtility.ts}`. They hold the working version of code that was refactored elsewhere (the new refactor wasn't working as it should, so the working copies were kept for reference). They are working reference implementations whose new homes are not yet verified; nothing under `src/` may import them, and the matching file should be deleted from `to-be-deleted/` once the new location is verified working.
- Tailwind 4 is CSS-first (`@import 'tailwindcss'` in `index.css`); there is no `tailwind.config.js`.
- `tsc -b` writes `tsconfig.tsbuildinfo` to the repo root; it is committed, not gitignored.
- Fonts load at boot from `public/fonts` (Inter variable) plus remote families in `src/lib/core/fonts.json`; requires network for the remote ones, best-effort via `Promise.allSettled`.
- **Deferred tests:** matrix compose/invert, world-to-local round trips, and rect intersection are NOT tested yet because that math still lives behind the CanvasKit `Matrix` in `Scene.ts`/`Shape.ts`. Add the tests once that math is extracted to the target `src/core` tier.
- **Running note (deliberately not fixed):** (a) `LayoutEngine` handles numeric `gridTemplateColumns`/`gridTemplateRows` at runtime but `nodeTypes.ts` `GridLayout` types them without `number`, so callers must cast; (b) `SnapManager.getSnapResult`'s third `gridSize` argument overwrites the value set via `setConfiguration`; (c) `roundingUtil` clamps the star corner radius on odd indices but not even ones (source marks it unfinished); (d) `TextEditor.splitAt` never updates the matching `indexMap` entry's `end`, so middle `insertText`/`deleteRange`/`applyStyle` operate on stale bounds and `deleteRange` can shrink `getLength()` without removing text — this buggy behavior is deliberately NOT pinned by tests; fix it before building on range edits; (e) `throttle` initializes `lastCall = 0`, so the first call within `limit` ms of the epoch is dropped; (f) `PCache.set` with `limit <= 0` inserts over capacity; (g) `getGradientPreview` sorts `gradient.stops` in place, mutating the caller's fill; (h) `EngineStateStore` has no `clear()`, `removeShapeData` notifies with `undefined`, and `createShapeData` silently overwrites an existing id; (i) `ResizeCursor` keys its cache by the unnormalized angle, so equivalent angles do not share entries; (j) `LayoutEngine` grid auto-resize is effectively dead — `requiredWidth`/`requiredHeight` algebraically equal the container size, so a grid never grows to fit children (only float round-off trips it); when the first child has no `dim`, auto-resize totals add a phantom gap, and `space-around`/`space-evenly`/`space-between` divide by `children.length` rather than the real-child count; (k) `getBackgroundStyleFromFillValue` returns `{ backgroundColor: null }` for a numeric `solid.color` (`colorValue` passes only strings through), and a `pattern` fill with a url is treated as an image `scaleMode: 'fill'` so its `repeat` is ignored; (l) `isPrintableCharUnicode` accepts a lone surrogate.
